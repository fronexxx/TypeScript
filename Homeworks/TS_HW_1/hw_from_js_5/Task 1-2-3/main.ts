// Task 1-2
let square2 = (a: number, b: number) => (a * b);
console.log(square2(10, 5));

// Task-2
let roundSquare2 = (radius: number): number => (Math.PI * radius * radius);
console.log(roundSquare2(5));

// Task-3
let squareCilindre2= (radius: number, height: number): number => (2 * Math.PI * radius * height);
console.log(squareCilindre2(5, 10));

